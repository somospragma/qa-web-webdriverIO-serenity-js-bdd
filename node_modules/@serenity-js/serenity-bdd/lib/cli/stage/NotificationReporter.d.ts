import type { Stage, StageCrewMember } from '@serenity-js/core';
import type { DomainEvent } from '@serenity-js/core/lib/events';
import type { Printer } from '../Printer';
/**
 * Notifies the user of any `Notification` or `Complaint` type artifacts being reported.
 *
 * @package
 */
export declare class NotificationReporter implements StageCrewMember {
    private readonly printer;
    private readonly stage?;
    constructor(printer: Printer, stage?: Stage);
    assignedTo(stage: Stage): StageCrewMember;
    notifyOf(event: DomainEvent): void;
}
//# sourceMappingURL=NotificationReporter.d.ts.map