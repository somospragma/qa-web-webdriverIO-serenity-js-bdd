import type { Stage, StageCrewMember } from '@serenity-js/core';
import type { DomainEvent } from '@serenity-js/core/lib/events';
import ProgressBar from 'progress';
import type { Printer } from '../Printer';
/**
 * Notifies the user of the progress of the Serenity BDD CLI jar download.
 *
 * @package
 */
export declare class ProgressReporter implements StageCrewMember {
    private readonly printer;
    private readonly stage?;
    private progressBar?;
    constructor(printer: Printer, stage?: Stage, progressBar?: ProgressBar);
    assignedTo(stage: Stage): StageCrewMember;
    notifyOf(event: DomainEvent): void;
}
//# sourceMappingURL=ProgressReporter.d.ts.map